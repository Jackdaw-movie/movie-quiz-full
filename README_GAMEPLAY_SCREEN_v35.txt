Movie Quiz – Gameplay Screen Expansion v35

Base: current v34 avatar master stage + v33 cinema master.

Projection surface in cinema master:
- approx. 1163 × 622 px in the 1672 × 941 master

Changes:
- functional selection/question UI now uses ~94–95% of #screen
- container query units are tied to the projection screen itself, not viewport
- difficulty grid uses 3 equal columns
- genre grid uses 3 × 2 layout
- question/answers area is widened; long answers wrap inside their cells
- Home/profile/settings positions are untouched
- HUD geometry is untouched
- cinema artwork and screen coordinates are untouched
