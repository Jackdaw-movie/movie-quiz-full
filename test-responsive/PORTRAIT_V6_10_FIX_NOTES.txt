Movie Quiz portrait v6.10

Car and car shine are unchanged from v6.9.

Fixes:
1. Shoes: global pointer safety check clears hover whenever the pointer is no longer
   physically over the booth hit target. pointerout/pointerleave/blur are also handled.
2. Booth: separate glow PNG is no longer used. The warm hover light is produced by
   CSS drop-shadow directly on the real booth.png, so it necessarily follows its alpha contour.
3. Marquee: rebuilt bulb-only groups from the portrait master. Detected bulbs: 59.
   Same desktop choreography: 1.35 s chase, delays 0 / -0.45 / -0.90 s, halo 3.2 s.
   Peak intensity is stronger on portrait so the animation remains readable after scaling.
4. Cache versions bumped to v6.10 / interaction v4.2.
